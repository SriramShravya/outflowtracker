# outflowtracker
Full-stack expense tracker built with HTML, CSS, PHP, and MySQL using XAMPP.

User login system with secure authentication.

Users can add expense amounts categorized by different sections.

Expenses are saved and updated in the database tables.

Displays total spending overall and per category.

Uses JSON to pass data from PHP to JavaScript.

Visualizes spending data with interactive Chart.js graphs.
